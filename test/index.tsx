import React from "react";
import ReactDOM from "react-dom";
import ReactChat from "../src/"

ReactDOM.render(<ReactChat title='Speech to text' welcome='Welcome to speech to text'></ReactChat>, document.getElementById("react-chat-container"))